# 网页笔记路由器隐私政策

生效日期：2026 年 7 月 25 日

## 1. 产品用途

网页笔记路由器用于在用户主动复制网页文字时，通过用户配置的 DeepSeek API
判断笔记分类，并将内容写入用户明确授权的本地 Markdown 目录。

## 2. 处理的数据

只有在用户主动选择网页文字并执行复制操作时，扩展才会处理以下数据：

- 用户选中并复制的网页文字；
- 当前网页标题和网址；
- 用户配置的路由标签、目标 Markdown 文件及模型设置；
- 用户自行提供的 DeepSeek API Key；
- 最近一次笔记保存结果。

扩展不会读取用户未主动复制的网页正文，不会收集密码、支付信息，也不会在后台持续记录浏览历史。

## 3. 数据用途和共享

用户复制的文字、当前网页标题和网址会发送至用户配置的 DeepSeek API，
唯一用途是生成笔记标题并选择路由标签。DeepSeek 对数据的处理受其自身隐私政策和服务条款约束。

除上述路由请求外，开发者不会将用户数据出售、出租、用于广告、信用评估或其他无关用途。

## 4. 本地存储

以下数据存储在用户本机的 Chrome 扩展存储中：

- DeepSeek API Key；
- API 地址和模型名称；
- 路由标签和文件配置；
- 启用状态及最近一次保存结果。

原始笔记内容写入用户通过系统目录选择器明确授权的本地目录。
开发者不运营用于接收或保存这些笔记内容的服务器。

复制的图片不会发送给 DeepSeek。图片二进制仅写入用户授权的本地附件目录；
DeepSeek 路由只使用复制的文字、网页标题和网址。纯图片笔记自动创建并使用本地“图片摘录”标签。

复制文字会先在本地进行密钥特征检测。检测到 API Key、Token、密码或私钥时，
该内容直接写入用户本地的 `密钥.md`，不会发送给 DeepSeek 或其他大模型服务。

## 5. 权限说明

- `storage`：保存用户设置和最近一次保存结果；
- `notifications`：显示保存成功或失败通知；
- `clipboardRead`：在用户主动复制时读取当次剪贴板文字和图片；
- `https://api.deepseek.com/*`：向 DeepSeek 发送用户主动复制的内容以完成分类；
- 网页内容脚本：仅监听用户主动执行的复制事件，并读取当次选中的文字、网页标题和网址；
- 本地目录权限：仅访问用户主动选择并授权的目录。

## 6. 数据控制与删除

用户可以随时：

- 在扩展设置中暂停自动收集；
- 删除或更换 API Key；
- 修改路由标签；
- 在 Chrome 扩展管理页面删除扩展，从而清除扩展本地设置；
- 自行删除本地目录中生成的 Markdown 内容；
- 通过浏览器站点或文件权限设置撤销目录授权。

## 7. 数据安全

扩展使用 HTTPS 与 DeepSeek API 通信。API Key 仅保存在本机扩展存储中，
但能够访问该 Chrome 用户配置或本机账户的人可能读取这些数据。用户应妥善保护自己的设备和 API Key。

## 8. 儿童隐私

本扩展不面向儿童，也不会有意收集儿童个人信息。

## 9. 政策更新

如果数据处理方式发生变化，本政策将同步更新生效日期和相关说明。

## 10. 联系方式

- 开发者：miao1564293969-lang
- 项目与问题反馈：https://github.com/miao1564293969-lang/web-note-router/issues
